import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Maps = () => {
  return (
    <View>
      <Text>Maps Page</Text>
    </View>
  );
};

export default Maps;

const styles = StyleSheet.create({});
