import React from 'react';
import {WebView} from 'react-native-webview';

const HomePage = () => {
  return <WebView source={{uri: 'https://smarttourismpemalang.codes'}} />;
};

export default HomePage;
