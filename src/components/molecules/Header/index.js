import {StyleSheet, Text, View} from 'react-native';

const Header = () => {
  return (
    <View>
      <Text>Header Js</Text>
    </View>
  );
};

export default Header;

const styles = StyleSheet.create({});
